node_modules/
lib/
*.tsbuildinfo
.pterodocs/
# The output directory before the rename; still ignored so an existing
# checkout does not suddenly show it as untracked.
.pterodoc/
.env

# The fixture site is a real Docusaurus site; its build artefacts are not source.
packages/docusaurus/test/fixtures/site/.docusaurus/
packages/docusaurus/test/fixtures/site/build/

# pterodocs's own documentation is a real Docusaurus site too.
packages/docs/.docusaurus/
packages/docs/build/

# Staged into packages/cli at pack time; the originals live at the repository root.
packages/cli/README.md
packages/cli/CHANGELOG.md

# The WordPress plugin: Prism is copied from npm, and the zip is a build output.
packages/wordpress/plugin/assets/vendor/
# Generated from packages/core/src/render/stylesheet.ts at build time.
packages/wordpress/plugin/assets/css/docs.css
packages/wordpress/plugin/pterodocs.zip
