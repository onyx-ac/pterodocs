# Credentials for the WordPress site. Copy to .env, which is git-ignored.
# The password is a WordPress Application Password, not the login password:
# Users, Profile, Application Passwords.
#
# Nothing is read from .env unless you name it: --env-file .env
WP_URL=https://onyx.ac
WP_USER=
WP_APP_PASSWORD=
